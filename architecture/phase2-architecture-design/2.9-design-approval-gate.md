# 2.9 Design Approval Gate

## Design Summary

### 6 Ports (35 methods total)
1. MessagingPort (10 methods) — fixes 9 violations
2. NPCCapability (7 methods) — fixes 4 violations
3. ArchivePort (6 methods) — fixes 3 violations
4. CampaignContextPort (5 methods) — fixes 3 violations
5. SettingsPort (3 methods) — fixes 4 violations
6. NotificationPort (4 methods) — fixes 8 violations

### 6 Adapters (thin delegates)
One per port. No logic. Wire in main.tsx.

### 1 Persistence Service
Wraps idb-keyval. Consolidates 11 scattered access points.

### 5 Boundaries
1. Domain↔State (ports)
2. Domain↔UI (notification port + service extraction)
3. State↔UI (notification port)
4. Persistence Gateway (service wrapper)
5. God File internal splits (9 targets)

### Coverage
- 31/53 violations fixed by ports
- 22/53 violations fixed by logic extraction (Phase 3)
- 53/53 total covered

## Gate Questions

1. **Is the design complete?** ✅ Yes — all violations covered
2. **Is the design evidence-backed?** ✅ Yes — every decision traces to Phase 0 data
3. **Is the design minimal?** ✅ Yes — 2 ports rejected, 1 method removed
4. **Is the design ready for implementation?** ✅ Yes — Phase 3 can begin

## Recommendation

Proceed to Phase 3 (Implementation) with this priority order:
1. Create 6 ports + 6 adapters + wire (fixes 31 violations)
2. Create persistence service (consolidates 11 files)
3. Extract logic from store slices (fixes 22 violations)
4. Split God Files (9 targets)
5. Add smoke tests (risk mitigation)

## Gate Status: ✅ PASS

Phase 3 (Implementation) is authorized.

## Conditions
- Smoke tests should be added BEFORE or DURING step 3 (logic extraction)
- Each step must pass: tsc + vite build + gate.mjs
- Each step: one commit, one push, one review
